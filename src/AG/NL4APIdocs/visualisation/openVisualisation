#!/bin/bash
echo "opening visualisation"
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
echo $DIR
cd $DIR/Visualisation/ChartsDemo-macOS/Build-command-line/Products/Debug
open ChartsDemo-macOS.app